# 📋 API ToDo List

API REST desenvolvida em **Java com Spring Boot** para gerenciamento de tarefas (ToDo List), permitindo criar, listar, atualizar e remover tarefas associadas a usuários. O projeto segue boas práticas de desenvolvimento, arquitetura em camadas, tratamento de exceções, documentação com **Swagger/OpenAPI** e execução em ambiente **Docker**.

---

## 🚀 Tecnologias Utilizadas

- Java 17  
- Spring Boot  
- Spring Web  
- Spring Data JPA  
- Bean Validation  
- Swagger / OpenAPI  
- Maven  
- Docker  
- PostgreSQL  

---

## 📁 Estrutura do Projeto

src/main/java/com/br/apiToDoList
│
├── controller # Controladores REST
├── service # Regras de negócio
├── repository # Repositórios JPA
├── model # Entidades
├── dto # DTOs
├── exception # Tratamento de exceções
└── config # Configurações (Swagger, segurança, etc.)


---

## 🧠 Modelo Entidade–Relacionamento (MER)

### Entidade: User

| Campo    | Tipo    | Descrição                  |
|--------|---------|----------------------------|
| idUser | Long    | Identificador do usuário   |
| name   | String  | Nome do usuário            |
| email  | String  | Email do usuário           |
| password | String | Senha criptografada        |
| role   | String  | Perfil (ADMIN ou USER)     |

---

### Entidade: Task

| Campo     | Tipo    | Descrição                       |
|----------|---------|---------------------------------|
| idTask   | Long    | Identificador da tarefa         |
| name     | String  | Nome da tarefa                  |
| description | String | Descrição da tarefa            |
| status   | String  | Status da tarefa                |
| dataTask | Date    | Data da tarefa                  |
| user     | User    | Usuário associado à tarefa     |

---

## 🗄️ Script de Criação do Banco de Dados (PostgreSQL)

```sql
--
-- PostgreSQL database dump
--

\restrict k862XOUTQNO7kuZdllVyeGrL90JvHaGGbRd1n5iz3yE1WNOrFjB2PXNBetsAYl2

SET statement_timeout = 0;
SET lock_timeout = 0;
SET idle_in_transaction_session_timeout = 0;
SET transaction_timeout = 0;
SET client_encoding = 'UTF8';
SET standard_conforming_strings = on;
SELECT pg_catalog.set_config('search_path', '', false);
SET check_function_bodies = false;
SET xmloption = content;
SET client_min_messages = warning;
SET row_security = off;

SET default_tablespace = '';
SET default_table_access_method = heap;

CREATE TABLE public.tasks (
    id_task bigint NOT NULL,
    description character varying(255) NOT NULL,
    name character varying(45) NOT NULL,
    status character varying(255) NOT NULL,
    id_user bigint,
    data_task date
);

ALTER TABLE public.tasks OWNER TO postgres;

ALTER TABLE public.tasks ALTER COLUMN id_task ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.tasks_id_task_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);

CREATE TABLE public.users (
    id_user bigint NOT NULL,
    email character varying(100) NOT NULL,
    name character varying(45) NOT NULL,
    password character varying(100) NOT NULL,
    role character varying(255),
    CONSTRAINT users_role_check CHECK (
        (role)::text = ANY (
            (ARRAY['ADMIN'::character varying, 'USER'::character varying])::text[]
        )
    )
);

ALTER TABLE public.users OWNER TO postgres;

ALTER TABLE public.users ALTER COLUMN id_user ADD GENERATED BY DEFAULT AS IDENTITY (
    SEQUENCE NAME public.users_id_user_seq
    START WITH 1
    INCREMENT BY 1
    NO MINVALUE
    NO MAXVALUE
    CACHE 1
);
⚙️ Configuração da Aplicação (PostgreSQL)
Exemplo de application.properties
spring.datasource.url=jdbc:postgresql://localhost:5432/todolist
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
▶️ Como Executar o Projeto (Sem Docker)
git clone https://github.com/pauloosmt/apiToDoList.git
cd apiToDoList
mvn spring-boot:run
A aplicação estará disponível em:

http://localhost:8080
🐳 Como Executar o Projeto com Docker
Build da imagem
docker build -t api-todolist .
Executar o container
docker run -p 8080:8080 api-todolist
Caso utilize PostgreSQL em container, recomenda-se o uso de Docker Compose.

📖 Documentação Swagger
A documentação interativa da API pode ser acessada em:

http://localhost:8080/swagger-ui/index.html
🔗 Endpoints da API
Criar tarefa
POST /tasks

{
  "name": "Estudar Spring Boot",
  "description": "Revisar JPA e Docker",
  "status": "PENDING",
  "dataTask": "2026-02-03"
}
Listar tarefas
GET /tasks

Buscar tarefa por ID
GET /tasks/{id}

Atualizar tarefa
PUT /tasks/{id}

Remover tarefa
DELETE /tasks/{id}

🧪 Exemplo via cURL
curl -X GET http://localhost:8080/tasks
❗ Tratamento de Erros
Exemplo de resposta de erro:

{
  "status": 400,
  "message": "Campo obrigatório inválido"
}
Códigos HTTP utilizados:

200 OK

201 Created

400 Bad Request

403 Forbidden

404 Not Found

500 Internal Server Error

📦 Versionamento
O projeto utiliza Git para controle de versão do código-fonte.

🧑‍💻 Autor
Paulo Taciano
GitHub: https://github.com/pauloosmt

📄 Licença
Projeto desenvolvido para fins educacionais.


---
